databaseChangeLog:
  - changeSet:
      id: Exclui registros de escolas para inserir com a coluna TipoCategoria
      author: Rafael
      preConditions:
        - onFail: MARK_RAN
        - sqlCheck:
            expectedResult: 1
            sql:  SELECT count(1) 
                  FROM INFORMATION_SCHEMA.TABLES 
                  WHERE TABLE_NAME = 'Escola' 
      changes:
        - sql:
            sql: |

              DELETE FROM "IntegracaoEscola";
              DELETE FROM "Escola";
  